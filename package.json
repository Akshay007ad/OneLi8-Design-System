{
  "name": "oneli8",
  "private": true,
  "version": "1.0.0-beta.3",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "sync:icons": "node packages/sync/src/build-icons.mjs",
    "sync:tokens": "node packages/sync/src/build-tokens.mjs",
    "build": "npm run sync:tokens && npm run sync:adapters && npm run sync:icons && npm run check",
    "check": "node packages/sync/src/check-structure.mjs",
    "sync:adapters": "node packages/sync/src/build-adapters.mjs"
  }
}
